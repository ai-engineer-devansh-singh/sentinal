fastapi>=0.110.0
uvicorn[standard]>=0.29.0
openai>=1.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
# CPU-only XGBoost (the default `xgboost` 3.2+ wheel pulls in the full
# nvidia CUDA stack — ~400 MB wasted on a CPU-only image).
xgboost-cpu>=2.0.0,<3.0
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0